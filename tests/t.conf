# The policy t.sh reads for this repository; the keys are in templates/t.conf
#
# check.sh is this repository's gate and sits at the root, not under tests/, so without
# this t.sh prove took it for code and took a commit's new checks away with its fix
tests     check.sh
