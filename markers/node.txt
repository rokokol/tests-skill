# Node (vitest, jest, node:test): lines that mean a run answered about less than you think.
#
# Opt in with `-m node`.

# the glob matched nothing and somebody allowed it
No test files found
--passWithNoTests
Tests:       0 total

# the file never executed, so its tests are absent rather than passing
Test suite failed to run

# the expectation was regenerated instead of checked
snapshot written
snapshots written

# a stored expectation nothing compares against any more. Taken from the totals line,
# `Snapshots:   1 obsolete, 1 passed, 1 total`, and not from the sentence above it: that
# sentence read `1 obsolete snapshot found` before jest 23 restructured every summary line,
# and it is being reworded again. The totals line survived both.
#
# It is safe only because jest omits a category whose count is zero — a healthy run says
# `Snapshots:   1 passed, 1 total` and never `0 obsolete`. jest promises that nowhere, so
# tests/upstream.sh keeps a healthy jest run with a matching snapshot among the runs every
# marker must stay quiet on: the day that habit changes, the weekly run says so instead of
# this quietly reddening every honest build.
obsolete,

# the run was silently reduced to one test

# something is still running; its state leaks into the next run
worker process has failed to exit gracefully
Jest did not exit one second after
