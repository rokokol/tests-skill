# Rust: lines that mean a run answered about less than you think.
#
# Opt in with `-m rust`. These are NOT in default.txt because a healthy workspace prints
# them: `running 0 tests` appears for every target without tests, doc-test targets
# included. Adopt this set in a crate where every target really is expected to have tests.

# nothing ran, and the exit status is still 0. A filter that matched nothing prints
# exactly these two lines as well, with `N filtered out` beside them — and `0 filtered
# out` is what every healthy full run prints, so that is not a marker: it was one, and
# it reddened every good `cargo test` until a healthy run was kept as a fixture
running 0 tests
test result: ok. 0 passed

# something happened that the summary line does not cover
panicked at
test result: FAILED
error: test failed, to rerun pass
